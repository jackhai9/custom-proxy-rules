# 个人自定义的规则 -- 有更改时需提交到github，会自动插入到所有配置文件的rules第一行
# 此文件在每次有更新需要提交到github之前，会通过pre-commit钩子自动执行merge-clash-rule.sh脚本
# 此脚本的功能是：把本文件的所有内容，插入到 $HOME/.config/clash/*.yaml 所有yaml的“rules:”下的第一行。
- DOMAIN-SUFFIX,velodata.app,DIRECT
- DOMAIN-SUFFIX,coinglass.com,DIRECT
